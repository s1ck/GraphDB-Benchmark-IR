# connection settings
host=localhost
port=3306
database=deu_news_2009_10k
username=root
password=######

# table definitions
table_words=words
table_co_n=co_n
table_co_s=co_s

# sets the treshold for number of outgoing edges
min_outdegree=20

# perform a warmup before running a benchmark
warmup=true

# select the queries to run 
query1=true
query2=true
query3=true