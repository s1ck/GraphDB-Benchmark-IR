# connection properties
host=localhost
port=3306
database=deu_news_2009_1M
username=root
password=

# table where the nodes (words) are stored
table_words=words

# table where the edges (neighbourhood co-occurrences) are stored
table_co_n=co_n

# table where the edges (sentence co-occurrences) are stored
table_co_s=co_s

# sets the treshold for number of outgoing edges
min_outdegree=20